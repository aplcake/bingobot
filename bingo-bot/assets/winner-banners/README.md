Drop your winner banner images here (PNG or JPG, 800×400 recommended).

The bot loads ALL images from this folder on startup and picks one at random for each winner announcement. The winner's name, trophies, and game title get overlaid on top.

If this folder is empty, the bot uses a generated dark/gold fallback.
